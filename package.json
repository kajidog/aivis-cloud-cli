{
  "name": "aivis-cloud-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "release-packages": "cd packages/npm && npm run build && cd ../.. && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6"
  }
}